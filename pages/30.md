```sql students_attendance
select
    student_name,
    attendance_rate
from student_attendance
order by student_name
```

```jsx
<BarChart
    data={students_attendance}
    x=student_name
    y=attendance_rate
    labels=true
    yFmt=pct2
    color="#3498db"
    height=300
/>
```