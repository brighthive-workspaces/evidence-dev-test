```sql students_attendance
SELECT
    student_name,
    attendance_rate
FROM students
ORDER BY attendance_rate DESC
```

```jsx
<BarChart
    data={students_attendance}
    x=student_name
    y=attendance_rate
    labels=true
    color="#1f77b4"
    height=400
    chartTitle="Students Attendance Rates"
/>
```