```sql 
select
    student_name,
    attendance_rate
from students
order by student_name
```


<BarChart
    data={student_attendance}
    x=student_name
    y=attendance_rate
    labels=true
    colorPalette={['#1f77b4']}
    xLabel="Student Name"
    yLabel="Attendance Rate (%)"
    yFmt=pct
    height=400
/>
