```sql student_attendance
select
    student_name,
    attendance_rate
from students
order by attendance_rate desc
```

```jsx
<BarChart
    data={student_attendance}
    x=student_name
    y=attendance_rate
    yFmt=pct
    labels=true
    colorPalette={['#4caf50']}
    height=300
/>
```