```sql students_attendance
SELECT
    student_name,
    attendance_rate
FROM students_attendance_records
ORDER BY student_name
```

```jsx
<BarChart
    data={students_attendance}
    x=student_name
    y=attendance_rate
    type=grouped
    labels=true
    xFmt="string"
    yFmt=pct
    colors={"#74c476"}
    height=400
/>
```