```sql student_attendance
SELECT
    student_name,
    attendance_rate
FROM
    students
ORDER BY
    student_name
```

```jsx
<BarChart
    data={student_attendance}
    x=student_name
    y=attendance_rate
    type=grouped
    yFmt=pct1
    labels=true
    height=400
/>
```